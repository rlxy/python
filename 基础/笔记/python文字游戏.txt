import  导入
random 模块       
secret  随机 
random.randint(a,b)用于生成一个指定范围内的整数。其中参数a是下限，参数b是上限
while  循环
guess 猜一猜
print  打印
“=”是赋值的意思
"\"是转义的意思
''' ''' 三重引号可以进行跨多行文本的打印



if条件：
	条件为真（True）执行的操作
else条件：
	条件为假（False）执行的操作

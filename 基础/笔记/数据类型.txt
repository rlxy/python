整型： 无论整数的大小长度为多少，统称为整型int。（整数没有小数点）
类型转换：   	转换符号：int() 
int()    将字符串数据类型转为int类型,  注：字符串内的内容必须是数字　
如果转换浮点数的话会直接把小数位直接舍去（注意：这里不是四舍五入，是直接舍去）


浮点型
浮点数：float()
将数值转换为小数



布尔类型
对于布尔值，只有两种结果即True和False，其分别对应与二进制中的0和1


字符串：str()
将括号内的转换为字符串
